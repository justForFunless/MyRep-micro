server:
  port: 7012
spring:
  application:
    name: micro-service2

eureka:
  instance:
    hostname: localhost #配置主机名
  client:
    service-url:
      defaultZone: http://localhost:7001/eureka/,http://localhost:7002/eureka/   #配置Eureka Server